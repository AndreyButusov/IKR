package IKR.model;

import java.time.LocalDate;

public class Cat extends Pets {
    public Cat(String name, LocalDate birthday, StringBuilder commands) {
        super(name, birthday, commands);
    }

}
